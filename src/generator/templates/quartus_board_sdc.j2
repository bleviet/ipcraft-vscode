# Quartus board-mode timing constraints for {{ entity_name }} on {{ board_name }}
# Generated by IPCraft
#
# Keyed on the board clock net name (as exposed by the board-top wrapper), not the IP's
# own clock port name — see quartus_sdc.j2 for the IP-OOC mode used by the virtual-pin
# timing project.

{% if board_clock %}
create_clock -period {{ board_clock.period_ns }} -name {{ board_clock.name }} [get_ports { {{ board_clock.name }} }]
{% else %}
# No board clock available.
{% endif %}
