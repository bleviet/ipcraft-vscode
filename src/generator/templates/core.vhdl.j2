{# VHDL Core Module Template #}
{# Generates bus-agnostic core logic with record-based register interface #}
--------------------------------------------------------------------------------
-- Entity: {{ entity_name }}_core
-- Description: Core logic module (bus-agnostic)
-- Generated by ipcraft VHDL Generator
--------------------------------------------------------------------------------

library ieee;
use ieee.std_logic_1164.all;
use ieee.numeric_std.all;
{% if uses_math_real %}
use ieee.math_real.all;
{% endif %}

use work.{{ entity_name }}_pkg.all;

entity {{ entity_name }}_core is
{% if generics | length %}
  generic (
{% for generic in generics %}
    {{ generic.name }} : {{ generic.type }}{{ " := " ~ generic.default_value if generic.default_value is not none else "" }}{{ ";" if not loop.last else "" }}
{% endfor %}
  );
{% endif %}
  port (
    -- Clock and reset
    clk : in std_logic;
    rst : in std_logic;

{% set has_regs_in = hw_registers | length > 0 or w1c_registers | length > 0 or sc_registers | length > 0 or cos_registers | length > 0 %}
{% if sw_registers | length > 0 or has_regs_in %}
    -- Register interface (record-based)
{% if sw_registers | length > 0 %}
    regs_in : in t_regs_sw2hw{{ ";" if has_regs_in or secondary_bus_ports | length > 0 or user_ports | length > 0 else "" }} -- SW-writable registers (from bus wrapper)
{% endif %}
{% if has_regs_in %}
    regs_out : out t_regs_hw2sw{{ ";" if secondary_bus_ports | length > 0 or user_ports | length > 0 else "" }} -- HW-status / W1C pulses / CoS values (to bus wrapper)
{% endif %}
{% endif %}
{% if secondary_bus_interfaces | length %}
{% for iface in secondary_bus_interfaces %}{% set iface_loop = loop %}

    -- {{ iface.name }} ({{ iface.mode | upper }})
{% for port in iface.ports %}
    {{ "%-14s"|format(port.name) }} : {{ "%-3s"|format(port.direction) }} {{ port.type }}{{ ";" if not loop.last or not iface_loop.last or user_ports | length else "" }}
{% endfor %}
{% endfor %}
{% endif %}
{% if user_ports | length %}

    -- User ports
{% for port in user_ports %}
    {{ port.name }} : {{ port.direction }} {{ port.type }}{{ ";" if not loop.last else "" }}
{% endfor %}
{% endif %}
    );
end entity {{ entity_name }}_core;

architecture rtl of {{ entity_name }}_core is

  ----------------------------------------------------------------------------
  -- Internal Signals
  ----------------------------------------------------------------------------
  -- Add your internal signals here

begin

  ----------------------------------------------------------------------------
  -- Main Process
  ----------------------------------------------------------------------------
  p_main : process(clk)
  begin
    if rising_edge(clk) then
      if rst = '1' then
        -- Reset logic
{% if has_regs_in %}
        regs_out <= C_REGS_HW2SW_RESET;
{% else %}
        null;
{% endif %}
      else
        -- Example: Access SW-writable register fields
        -- if regs_in.control.enable = '1' then
        --     -- Do something
        -- end if;

        -- Example: Update HW-writable register fields
        -- regs_out.status.busy <= '1';
        null;
      end if;
    end if;
  end process;

end architecture rtl;
