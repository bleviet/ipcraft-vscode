## What changed

<!-- Summary of the change. -->

## Why

<!-- The problem this solves, or link the issue it closes (e.g. "Closes #123"). -->

## How it was validated

<!-- Commands run and their results. Delete rows that don't apply. -->

```bash
npm run lint
npm run type-check
npm run test:unit
npm run compile
```

- [ ] `npm run lint` — zero warnings
- [ ] `npm run type-check`
- [ ] `npm run test:unit`
- [ ] `npm run compile`
- [ ] `npm run test:e2e` (extension-host / VS Code command changes)
- [ ] `npm run test:browser` (editor UI / webview changes)
- [ ] Relevant `npm run test:integration:*` (generator template / HDL / vendor output changes)
- [ ] Tested manually in the Extension Development Host (UI changes)

## Known follow-up work

<!-- Anything intentionally left out of scope, or none. -->
